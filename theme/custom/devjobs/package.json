{
  "private": true,
  "scripts": {
    "watch": "npx tailwindcss --input src/tailwind.css --output dist/tailwind.css --watch",
    "build": "npx tailwindcss --input src/tailwind.css --output dist/tailwind.css --minify"
  },
  "dependencies": {
    "postcss": "^8.4.5",
    "tailwindcss": "^3.0.12"
  }
}
